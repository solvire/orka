# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.env
.venv/
env/
venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Orka local data
.orka/

# Environment files
.env.local
.env.production
.env.*.local

# ── Secrets & credentials ──────────────────────────
# Never commit real keys, certs, or secrets to the repo.
# (example.env is intentionally tracked — it contains only placeholders)
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*.ppk
*.pka
secrets/
credentials/
.creds/
**/credentials.json
**/service-account*.json
**/sa-key*.json

# Checkpoint artifacts (optional: comment out to exclude from version control)
# docs/checkpoints/

# detect-secrets baseline
.secrets.baseline

# Safety CLI
.safety-project.ini

.aider*
.orka_*

# ── Kilo config (agents/commands/scripts are tracked; secrets & state are not) ──
.kilo/kilo.json
.kilo/kilo.jsonc
.kilo/agent-manager.json
.kilo/worktrees/
.kilo/node_modules/
.kilo/package.json
.kilo/package-lock.json
kilo.json
kilo.jsonc
tmp/
